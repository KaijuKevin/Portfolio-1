<%= render('layouts/errors', :object => @project) %>

<%= bootstrap_form_for [@category, @project] do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :link %>
  <%= f.text_field :description %>
  <p><%= f.submit %></p>
<% end %>
